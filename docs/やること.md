# やること

> 最終更新: 2026-05-16

---

## ✅ 修正済み（2026-05-16 コードレビューで対応）

| # | 箇所 | 内容 |
|---|---|---|
| R1 | `app/mini-games/rumble/page.tsx` | バトル参加後に BP残高 UIを-100即時反映 |
| R2 | `app/api/minigames/rumble/daily-result/route.ts` | GASボディに `key` を追加 |
| R3 | `app/api/minigames/rumble/spectator/route.ts` | GASボディに `key` を追加 |
| M1 | `app/music2/page.tsx` | 歌詞インライン編集「保存」で `displayLyrics` state を更新 |

---

## ✅ 実装済み（2026-05-16）

### refcodeから紹介者を自動振り分け（admin/finance）

### 背景
- `applies`シートの`ref_code`列に紹介コードが入っているユーザーがいる
- しかし紹介者の自動解決（`referrer_login_id`への反映）は「承認時」にしか走らない
- 過去ユーザーや後からref_codeを手入力した場合は反映されないまま
- `/admin/finance` の紹介ツリーで手動ドラッグ以外の方法で一括更新したい

### 実装内容

#### 1. GAS: `ref_backfill_from_refcode` アクション（`gas/Code.gs`）

- `applies`シート全行を走査
- 条件: `ref_code`が入っていて、かつ`referrer_login_id`が空の行のみ対象（既存設定は上書きしない）
- 各行の`ref_code`で`my_ref_code`を検索 → 一致したら紹介者を解決
- 更新カラム: `referrer_login_id` / `referrer_2_login_id` / `referrer_3_login_id` / `ref_path`
- `ref_events`シートにも監査ログを記録（`appendRefEvent_`を使用）
- レスポンス: `{ ok: true, updated: N, skipped: N, unmatched: N }`
  - `updated`: 紹介者を新たに設定した件数
  - `skipped`: `referrer_login_id`が既に設定済みでスキップした件数
  - `unmatched`: `ref_code`はあるが対応する`my_ref_code`が見つからなかった件数

#### 2. Next.js APIルート: `app/api/admin/ref-backfill/route.ts`

- `POST /api/admin/ref-backfill`
- Admin Basic Auth（`middleware.ts`が自動でカバー）
- GAS `ref_backfill_from_refcode` アクションを `GAS_ADMIN_KEY` 付きで呼び出す
- レスポンスをそのままフロントへ返す

#### 3. UI: `app/admin/finance/TreeTab.tsx` にボタン追加

- ボタンラベル: 「🔗 refcodeから紹介者を更新」
- 押下 → `POST /api/admin/ref-backfill` を呼び出す
- ローディング中はボタンを無効化
- 結果を画面に表示:
  - 成功: 「✅ XX件更新、YYスキップ（設定済み）、ZZ件不一致（ref_code未登録）」
  - エラー: エラーメッセージを赤字で表示
- ボタン位置: TreeTabの「ツリー再生成」ボタン群の近く

### 注意点
- `referrer_login_id`が既に設定されている行は**上書きしない**（手動設定・ドラッグ割当を保護）
- 強制上書きが必要な場合は別途 `force=true` パラメータの追加を検討
- `ref_tree_build` は別アクション — backfill後に手動でツリー再生成が必要

### 関連ファイル
| ファイル | 役割 |
|---|---|
| `gas/Code.gs` | `ref_backfill_from_refcode` アクション追加 |
| `app/api/admin/ref-backfill/route.ts` | 新規APIルート |
| `app/admin/finance/TreeTab.tsx` | ボタンUI追加 |
| `app/api/admin/ref-reassign/route.ts` | 参考: 既存の1件ずつ再割当API |

---

## ✅ 実装済み（2026-05-16）アフィリエイト月次集計 Phase 1

仕様書: `docs/superpowers/specs/2026-05-16-affiliate-monthly-summary-design.md`

| # | ファイル | 内容 |
|---|---|---|
| AF-1 | `gas/Code.gs` | `getSystemSettings_()` 追加 |
| AF-2 | `gas/Code.gs` | `affiliate_monthly_summary` アクション追加 |
| AF-3 | `app/api/admin/affiliate-summary/route.ts` | 新規 API ルート |
| AF-4 | `app/admin/finance/MonthlyTab.tsx` | 月次集計タブ UI 新規作成 |
| AF-5 | `app/admin/finance/page.tsx` | 「月次集計」タブ追加 |

> ⚠️ GAS デプロイ + `system_settings` シート手動作成が必要

---

## 🔲 未実装TODO

### ~~紹介チェーン 5段拡張（bunpai.md Tasks 2・3・4・8）~~ ✅ 実装済み（2026-05-16）

| Task | 内容 |
|---|---|
| Task 1 | `getSystemSettings_()` 追加（AF-1と同時実装） |
| Task 2 | `approveRowCore_` の `ensureCols_` に `referrer_4/5`, `affiliate_granted_at` 追加 |
| Task 3 | `resolveRefChain_()` を5段対応に拡張（後方互換・循環防止付き） |
| Task 4 | `approveRowCore_` の chain 書き込みを5段に拡張 |
| Task 8 | `admin_list` レスポンスに `referrer_4/5`, `affiliate_granted_at` 追加（既に実装済みを確認） |
| Tasks 5〜7 | 自動EP付与 — 「手動配布」方針のためスキップ |

> ⚠️ GAS デプロイが必要

#### Phase 2（内部CC決済）— CC実装後

| # | 内容 |
|---|---|
| AF-6 | Stripe Webhook → GAS `cc_payment_record` → `wallet_ledger` に `kind:"cc_payment"` 記録 |
| AF-7 | `affiliate_monthly_summary` の Phase 2 コメントアウト解除 |
| AF-8 | `MonthlyTab.tsx` の CC 列を実値表示に切り替え |

---

## 🔲 残課題（低優先度）

### ランブル: handlePrevPlay でプレイヤー脱落状態が更新されない
- `handlePrevPlay` の `batch_eliminate` 処理が `addLog` のみで `setPrevSpectatorPlayers` を呼ばない
- 現状バトルログモーダルにプレイヤータグ表示がないため無症状
- 将来的に前回バトルのプレイヤータグ表示を追加する場合に対応が必要
- 対応: `prevSpectatorPlayers` stateを追加し、`handlePrevPlay` で `eliminated` 更新を実装

### ランブル: 観戦タブuseEffectの過剰評価
- `spectatorData` 等が依存配列に含まれ、setStateのたびにuseEffectが再評価される
- 実害はないが、refを使った管理に切り替えると効率化できる

### 紹介ダッシュボード: fetched=true後は再フェッチしない
- ページリロードなしで最新データが取れない
- 通常の使い方では問題なし
